<?xml version="1.0" encoding="utf-8"?>
<!-- Frosted glass tint over the greeting banner's background photo: a
     semi-transparent white fill plus a thin white border. Corners set to 0dp
     for rectangular banner appearance (no rounded card styling). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/glass_overlay_fill" />
            <corners android:radius="0dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="1dp"
                android:color="@color/glass_overlay_border" />
            <corners android:radius="0dp" />
        </shape>
    </item>
</layer-list>
