<?xml version="1.0" encoding="utf-8"?>
<!-- Back arrow + title, shared by every Profile sub-screen (Personal
     Information, Change Password, Valid ID, Upload New ID). Sits directly on
     the gradient background, so it uses the on-screen text color. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/headerBackButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="8dp"
        android:src="@drawable/ic_arrow_back"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/button_back" />

    <TextView
        android:id="@+id/headerTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:textColor="?attr/textOnScreen"
        android:textSize="17sp"
        android:textStyle="bold" />

</LinearLayout>
