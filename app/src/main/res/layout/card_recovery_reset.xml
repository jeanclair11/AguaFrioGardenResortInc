<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="32dp"
    android:paddingEnd="24dp"
    android:paddingBottom="32dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="serif"
        android:text="@string/recovery_heading"
        android:textColor="?attr/textPrimary"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/recovery_reset_subheading"
        android:textColor="?attr/textMuted"
        android:textSize="13sp" />

    <TextView
        style="@style/FieldLabel"
        android:layout_marginTop="24dp"
        android:text="@string/label_new_password" />

    <LinearLayout style="@style/InputField">
        <ImageView style="@style/InputIcon" android:src="@drawable/ic_lock" android:contentDescription="@null" />
        <EditText
            android:id="@+id/newPasswordField"
            style="@style/InputEditText"
            android:hint="@string/hint_password"
            android:inputType="textPassword" />
        <ImageView
            android:id="@+id/newPasswordToggle"
            style="@style/InputTrailingIcon"
            android:src="@drawable/ic_eye"
            android:contentDescription="@null" />
    </LinearLayout>

    <TextView
        style="@style/FieldLabel"
        android:layout_marginTop="16dp"
        android:text="@string/label_confirm_new_password" />

    <LinearLayout style="@style/InputField">
        <ImageView style="@style/InputIcon" android:src="@drawable/ic_lock" android:contentDescription="@null" />
        <EditText
            android:id="@+id/confirmPasswordField"
            style="@style/InputEditText"
            android:hint="@string/hint_confirm_password"
            android:inputType="textPassword" />
        <ImageView
            android:id="@+id/confirmPasswordToggle"
            style="@style/InputTrailingIcon"
            android:src="@drawable/ic_eye"
            android:contentDescription="@null" />
    </LinearLayout>

    <TextView
        android:id="@+id/resetError"
        style="@style/FieldError" />

    <Button
        android:id="@+id/resetPasswordButton"
        style="@style/PrimaryButton"
        android:layout_marginTop="22dp"
        android:text="@string/reset_password_caps" />

    <TextView
        android:id="@+id/backToLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="22dp"
        android:background="?android:attr/selectableItemBackground"
        android:padding="6dp"
        android:text="@string/back_to_login_caps"
        android:textColor="?attr/accentPrimary"
        android:textSize="13sp"
        android:textStyle="bold" />
</LinearLayout>