<?xml version="1.0" encoding="utf-8"?>
<!-- "Account Recovery" content, swapped inside the fixed white card. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="32dp"
    android:paddingEnd="24dp"
    android:paddingBottom="32dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="serif"
        android:text="@string/recovery_heading"
        android:textColor="@color/text_dark"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/recovery_subheading"
        android:textColor="@color/text_muted"
        android:textSize="13sp" />

    <TextView
        style="@style/FieldLabel"
        android:layout_marginTop="24dp"
        android:text="@string/label_registered_email" />

    <LinearLayout style="@style/InputField">

        <ImageView style="@style/InputIcon" android:src="@drawable/ic_email" />

        <EditText
            android:id="@+id/emailField"
            style="@style/InputEditText"
            android:hint="@string/hint_registered_email"
            android:inputType="textEmailAddress" />
    </LinearLayout>

    <!-- Send OTP (text + arrow, styled like the primary button) -->
    <LinearLayout
        android:id="@+id/sendOtpButton"
        style="@style/PrimaryButton"
        android:layout_marginTop="22dp"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/send_otp"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:textStyle="bold" />

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_arrow_forward" />
    </LinearLayout>

    <TextView
        android:id="@+id/backToLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="22dp"
        android:background="?android:attr/selectableItemBackground"
        android:padding="6dp"
        android:text="@string/back_to_login"
        android:textColor="@color/accent_orange"
        android:textSize="14sp"
        android:textStyle="bold" />
</LinearLayout>
