<?xml version="1.0" encoding="utf-8"?>
<!-- Shared resort header. Included at the top of every auth screen. -->

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="250dp">

    <!-- Background photos: MainActivity picks the pair for the active theme
         and slowly crossfades between them (only the overlay's alpha moves;
         everything else in the header stays fixed). -->
    <ImageView
        android:id="@+id/headerPhoto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/agua_night_mode"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />

    <ImageView
        android:id="@+id/headerPhotoOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />

    <!-- Dark Gradient Overlay -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_gradient_overlay" />

    <!-- Header Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingTop="30dp"
        android:paddingBottom="40dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp">

        <!-- Logo -->
        <FrameLayout
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_marginTop="5dp"
            android:background="@drawable/agua_frio_logo"
            android:padding="10dp" />

        <!-- Resort Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="0dp"
            android:fontFamily="serif"
            android:gravity="center"
            android:letterSpacing="0.02"
            android:text="@string/resort_title"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="-5dp"
            android:letterSpacing="0.18"
            android:text="@string/resort_subtitle"
            android:textColor="?attr/accentPrimary"
            android:textSize="11sp"
            android:textStyle="bold" />

    </LinearLayout>

</FrameLayout>